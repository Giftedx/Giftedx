name: Check README

on:
  # main only: an unrestricted push trigger ran this twice for every pull
  # request — once for the branch push, once for the pull request itself.
  push:
    branches: [main]
  pull_request:

# A superseded run wastes a runner without this.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run selftest
        run: python tools/check-readme.py --selftest

      - name: Check README.md
        run: python tools/check-readme.py README.md
